v1.4.74:
* Keep device reauthentication attached to its signed Tracker destination when a browser or identity-provider handoff drops the return query, so successful organization sign-in can restore the existing tablet authorization instead of landing on the team home page.

v1.4.73: deployed
* Give every delegated organization administrator read-only access to their organization's audit trail from Administration home, and identify organization-user actions by member name and email instead of only an internal identifier.

v1.4.72: deployed
* Distinguish historical QR uses from the current tablet inventory and link the current-device count directly to Enrollment authorizations.

v1.4.71: deployed
* Restore the current RID2Caltopo distribution recommendations to Android build 137 and Apple build 143; tracker functionality release 148 remains a separate protocol capability and must not trigger an app-update warning.

v1.4.70: deployed
* Let delegated enrollment administrators see and change the Closed or Open enrollment access mode alongside the QR controls they already manage.

v1.4.69: deployed
* Keep only the newest authorization for each RID2Caltopo installation, including devices that re-enroll while an earlier credential is still awaiting member authentication, and clean up legacy blank-ID duplicates without merging distinct modern devices that share a name.
* Put the single active Drone-team enrollment QR at the bottom of Administration home, remove the duplicate QR surface from Enrollment authorizations, and present explicit Closed member-verified and Open QR-possession access modes with their replacement consequences.

v1.4.68: deployed
* Reorganize organization administration into focused configuration, flight records, members and roles, enrollment authorization, and audit pages while keeping service status and the current enrollment QR on the landing page.
* Display organization-administration timestamps in each operator's browser time zone, and show only the current authorization for each RID2Caltopo installation instead of duplicate device-name entries.
* Allow only one active enrollment QR per organization, bind member-verified installations to an authenticated authorized user, require that same user after a device is reported lost, and make QR-managed replacement revoke prior device access with an explicit compromise warning.

v1.4.67: deployed
* Keep device reauthentication attached to its Google sign-in callback, return successful operators to RID2Caltopo automatically, and distinguish duplicate tablet enrollments in the administrator inventory.

v1.4.66: deployed
* Let locally installed Android field-test builds open Tracker enrollment QR links directly by publishing their signing certificate alongside the Play and direct-release certificates.

v1.4.65: deployed
* Clarify reauthentication quarantine: RID2Caltopo remains usable offline, independent CalTopo credentials survive, and Tracker-issued CalTopo credentials are disabled until an authorized user restores access.

v1.4.64: deployed
* Correct the public release history so the superseded v1.4.62 note remains separate from the deployed reauthentication repair.

v1.4.63: deployed
* Replace immediate device-configuration wiping with a recoverable reauthentication quarantine: preserve RID and CalTopo settings, let operators retry Google account selection, restore the existing credential for an authorized r2c_device user, and offer explicit erasure only after three unsuccessful identities.

v1.4.62:
* Superseded by v1.4.63 to record the normalized secret-scan baseline produced by release qualification.

v1.4.61: deployed
* Add the r2c_device member permission for RID2Caltopo operators and automatically include records_viewer whenever it is assigned; NCSSAR enrollment remains open during verification.

v1.4.60:
* Superseded before deployment after release qualification normalized the tracked secret-scanner baseline.

v1.4.59: deployed
* Introduce tracker functionality release 148 independently of Android and Apple distribution build numbers while leaving minimum-version enforcement disabled during NCSSAR field verification.
* Let organization administrators require a lost or misplaced RID2Caltopo device to reauthenticate, immediately block its tracker access, and instruct Android and Apple clients to clear managed RID-map, tracker, and CalTopo credentials.
* Prepare unauthorized-user request email notifications with a direct link to the organization's administration page; closed enrollment remains disabled until its membership workflow is completed and verified.

v1.4.58:
* Superseded before deployment after release qualification normalized the tracked secret-scanner baseline.

v1.4.57: deployed
* Make tracker enrollment QR codes launch RID2Caltopo directly on Android and Apple, retain a browser fallback, and publish verified mobile-app association metadata.

v1.4.56: deployed
* Accept App Store Connect's delivered test-ping event and email a clearly labeled end-to-end confirmation, while continuing to send separate alerts for user crash and screenshot feedback.

v1.4.55: deployed
* Correct the public release history so superseded v1.4.53 notes remain separate from the deployed v1.4.54 summary.

v1.4.54: deployed
* Warn organization administrators before R2C tablet authorizations expire, show a critical coordination-outage callout when none remain usable, and let authorized administrators extend one or every renewable tablet credential without rescanning.
* Let administrators renew an unrevoked enrollment campaign for seven days and explicitly require downloading the newly signed QR before sharing it.
* Accept signed App Store Connect webhooks for TestFlight crash and screenshot feedback, suppress duplicate deliveries, and email prompt feedback alerts to the platform administrator.
* Identify the organizations, tablets, requesters, and browser sessions responsible when the guarded release activity check blocks a deployment.

v1.4.53:
* Superseded before deployment.
* Warn organization administrators before R2C tablet authorizations expire, show a critical coordination-outage callout when none remain usable, and let authorized administrators extend one or every renewable tablet credential without rescanning.
* Let administrators renew an unrevoked enrollment campaign for seven days and explicitly require downloading the newly signed QR before sharing it.

v1.4.52: deployed
* Replace Preview pending with the first live tablet thumbnail as soon as it arrives, without requiring a tracker page reload.

v1.4.51: deployed
* Snapshot each completed link test's source capabilities so fluctuating tablet frame-rate advertisements cannot invalidate a quality choice already shown to the requester.
* Return disconnected tablet stream pages to the organization stream list and add direct Back to streams navigation on tablet-scoped pages.

v1.4.50: deployed
* Accept Cloudflare's attributed single-object TURN credential response so consent-safe preflight receives authenticated relay candidates.

v1.4.49: deployed
* Restore consent-safe video preflight by attaching attributed Cloudflare credentials to returned TURN relay URLs and rejecting incomplete relay configurations.
* Put Cancel request directly beside a live session owned by the requester while preserving another viewer's in-progress request.

v1.4.48: deployed
* Automatically refresh organization configuration requests under the production browser security policy, bring completed proposals into view, and let administrators cancel a request while waiting for its device.

v1.4.47: deployed
* Identify connected RID2Caltopo build numbers before requesting organization configuration, require build 134 or later, and turn incompatible or disconnected pending requests into clear, discardable errors instead of waiting forever.
* Keep delegated-role help bubbles fully visible below their icons instead of allowing the members table header to clip the explanation.

v1.4.46: deployed
* Replace dated trials and dormant payment capability with open-ended extended-beta access, a platform-funded $10 calendar-month allowance, projected and exceeded notices, and a video-only cutoff at $9 while flight logs and R2C owner arbitration continue.
* Add a dedicated, filterable platform audit log with CSV export, a 90-day default review window, 365-day retention, explicit retention holds, access auditing, and daily cleanup.
* Attribute Cloudflare TURN credentials by opaque organization identifier and cache credentials separately per organization for provider-cost reconciliation.
* Complete approved recording downloads without leaving the current-session page and drive transfer-state reconciliation from the existing organization streams event channel instead of periodic polling.
* Let organization configuration administrators pull verified CalTopo credentials and drone lists from a connected RID2Caltopo device, review the differences, publish an approved timestamped release, and restore earlier releases.
* Automatically offer the current approved organization configuration to compatible Android and Apple clients while sending nothing until an administrator publishes the first release.
* Add the delegable config_admin role and plain-language help bubbles describing every organization role without granting configuration administrators unrelated member, billing, policy, or flight-record authority.
* Make complete pre-publication qualification the documented local gate, retain the rapid presentation-only testing path, refresh the tracked secret baseline, and update GitHub security actions for the current Node runtime.

v1.4.45:
* Superseded before deployment.
* Replace dated trials and dormant payment capability with open-ended extended-beta access, a platform-funded $10 calendar-month allowance, projected and exceeded notices, and a video-only cutoff at $9 while flight logs and R2C owner arbitration continue.
* Add a dedicated, filterable platform audit log with CSV export, a 90-day default review window, 365-day retention, explicit retention holds, access auditing, and daily cleanup.
* Attribute Cloudflare TURN credentials by opaque organization identifier and cache credentials separately per organization for provider-cost reconciliation.
* Complete approved recording downloads without leaving the current-session page and drive transfer-state reconciliation from the existing organization streams event channel instead of periodic polling.

v1.4.44: deployed
* Make an authenticated recording upload authoritative after tablet approval so delayed or lost websocket acknowledgements cannot strand Android or iOS downloads.
* Add transfer-state diagnostics and accept websocket approval idempotently when the HTTP upload arrives first.
* Let an explicitly acknowledged fast publication carry qualified non-presentation changes while retaining exact-commit qualification, a zero-traffic candidate, cloud regression, health checks, and automatic rollback.

v1.4.43: deployed
* Close negotiated video sessions when no RTP packets arrive instead of waiting forever for a browser playback event that cannot occur.

v1.4.42: superseded before deployment
* Put the active live-video player first, separate Live sessions from Recorded sessions, and clear the waiting-for-first-frame message once decoded video is playing.
* Add an explicit presentation-only release shortcut that retains local qualification, zero-traffic candidate verification, health checks, and rollback while clearly requiring --bypass-safety-checks to omit idle and hosted-staging safeguards.

v1.4.41: deployed
* Expire unanswered live-video and recording-transfer approval requests after 60 seconds, while keeping approved playback and upload windows independently bounded.
* Reconcile tablet stream and pending-transfer state when an R2C websocket reconnects, without adding periodic tracker polling.
* Keep recording-transfer progress visible after tablet approval and simplify the current-session recording actions to Play and Download.
* Restore delivery of live-video requests to connected tablets and reject stale signaling cleanly.

v1.4.40: deployed
* Keep superseded, never-deployed release notes separate from the current tracker summary on the public version-history page.

v1.4.39: deployed
* Keep recording downloads enabled while treating tracker-local media as a temporary transfer spool that is deleted after the browser response completes.
* Expire abandoned recording transfer spools instead of retaining completed media, and remove tracker-side playback of transferred copies.

v1.4.38:
* Superseded before deployment.
* Disable tracker-side recording transfer, storage, download, and transferred-copy playback until an explicit storage and retention policy is established; tablet-side recordings and remote playback remain available.

v1.4.37:
* Superseded before deployment.
* Complete an approved recording transfer as one browser action by starting the file download automatically when the tablet upload finishes.
* Place Download recording beside Play recording, and let authorized users play the tracker-transferred copy without reconnecting to the tablet.

v1.4.36: deployed
* Let authorized organization video users download a tablet recording as an ordinary browser file, with tablet approval when Remote Video Control is off and automatic transfer when it is on.
* Transfer recordings from Android and iOS in resumable chunks, retain completed tracker copies for later authorized downloads, and leave the tablet's original recording untouched.
* Keep active WebRTC requests intact while tablet stream membership changes, and promptly clear completed, failed, or pilot-terminated requests from the current-session page.
* Publish completed tablet recordings from the recorder's file-closed event so newly captured video appears without relying on a timing delay or manual refresh.

v1.4.35: deployed
* Show the tablet's actual managed-video stop or source-attachment reason after signaling ends instead of replacing it with a generic unavailable-status message.
* Refresh stale viewer and Request in progress controls as soon as a managed-video request reaches a terminal state.

v1.4.34: deployed
* Added Microsoft Entra OpenID Connect for invited organization members, with authorization-code PKCE, tenant-aware token verification, stable identity binding, and no automatic membership or role provisioning.
* Added managed-video diagnostics across approval, browser offer delivery, WebRTC state, codec negotiation, packet receipt, frame decoding, and HTML video playback without changing the media path.

v1.4.33: deployed
* Allow an unexpired pending member to activate through ordinary organization Google sign-in when Google verifies the exact invited email, matching the guidance already shown on the member administration page.
* Make newly archived CalTopo track descriptions point to one stable recording-session link instead of a drone-name filter that could list several videos; legacy broad stream links remain supported.
* Show each recording's incident-local start date and time.
* List every authenticated connected R2C tablet on the organization streams page, with a direct tablet link even when the device currently advertises no live or recorded streams.
* Let organization user administrators restore legacy archived members directly while preserving their assigned roles, issuing a fresh seven-day activation, and emailing the member-specific activation link.
* Send activation invitations when ordinary organization members are added so pending accounts can complete password or Google activation.
* Add a Send invitation control beside pending members that renews the single-use link for seven days, emails it to the member, and records the action in the organization audit history.

v1.4.32: deployed
* Made organization entry role-aware: records viewers land on their organization dashboard, video-only requesters land on streams, administrative roles land on administration, and non-admin members are denied direct administration access.
* Added visible signed-in or Guest identity on the organization directory, plus direct sign-in controls for guests on both the directory and organization pages.
* Kept tablet-specific Remote Video Control guidance accurate during transient empty stream advertisements by retaining the setting on the authenticated live tablet connection.
* Reduced local release qualification time without removing coverage by running the complete unit suite once and executing the independent runtime/migration and security gates in parallel.
* Further reduced local qualification time by running dependency, static-analysis, secret-scanning, and SBOM security checks concurrently, while retaining separate results and failure handling for every check.
* Removed the redundant immediate candidate retest from the release procedure; the candidate is still tested during deployment and freshly retested immediately before promotion.
* Added explicit, validated reuse of isolated staging resources within a 24-hour refresh window while continuing to refresh both production database clones and rerun the complete hosted regression for every candidate.
* Parallelized the independent tracker and control-plane database dump and restore operations, with separate failure checks and cleanup of interrupted jobs.
* Added an explicit stream-events unsubscribe acknowledgement so release tests and clients can close long-lived organization event sessions promptly.
* Made guarded Cloud Run builds use a clean archive of the exact tagged commit, reducing upload context and preventing local editor artifacts from aborting source enumeration.

v1.4.31: deployed
* Fixed the authenticated organization chooser so its dialog opens under the production Content Security Policy instead of relying on blocked inline JavaScript.

v1.4.30: deployed
* Added requester-controlled remote video for tablets that explicitly advertise the persistent Remote Video Control setting, while preserving pilot/VO approval when that setting is off.
* Show the measured Direct/Routed link result and selectable bandwidth-safe frame sizes and rates to the requester, while continuing to enforce one viewer per RID2Caltopo device.
* Show the current viewer on tablet session pages and disable additional Request Video actions while a remotely controlled stream is in use.
* Made organization member names open a direct editor for names, email addresses, and roles, with auditable disable and restore actions that preserve member history.
* Kept authenticated organization identity across public dashboards and the tracker home page, where Google-authenticated users can select any active organization membership authorized for their verified email while password sessions remain tenant-scoped.

v1.4.29: deployed
* Replaced repeated current-session page reloads with authenticated membership reconciliation: the page now reloads only when stream session IDs change and swaps refreshed preview frames in place.
* Added focused-viewer thumbnail leases so connected Android and iOS tablets refresh live previews every five seconds only while the tablet stream page is being viewed, without starting a full video session or storing media in the cloud.
* Stopped volatile source dimensions, frame rate, bitrate, and codec observations from being treated as stream lifecycle changes.

v1.4.28: deployed
* Stabilized the current-session streams page around a clearly labeled single session snapshot, avoiding repeated full-page reloads while link testing and waiting for pilot/VO approval.
* Count video as started only after the browser receives media, retain the browser-side WebRTC termination reason, and report a clear no-packets failure instead of briefly presenting an empty track as playing.

v1.4.27: deployed
* Added an IAM-protected Google Cloud staging release path that refreshes isolated clones of both pilot databases, creates test credentials only inside the clone, runs authenticated regression there, and promotes the exact tested image digest through the existing zero-traffic production candidate and rollback workflow.
* Added ephemeral short tablet and stream links, with authenticated current-session listings, thumbnails, live viewing, and playback of recently captured streams while the RID2Caltopo device remains connected.
* Kept live streams available through the tablet link before telemetry identifies a drone; CalTopo Live and Archived Track links are added only when the app can match that stream to telemetry, with captured-video links retained after track completion.
* Limited each RID2Caltopo device to one remote viewer, with pilot/VO-controlled rejection or higher-priority replacement and clear requester-facing outcomes.

v1.4.26: deployed
* Removed the retired unscoped `/ws/r2c`, `/upload`, `/r2c`, and `/ws` surfaces plus unused public API-documentation routes; device coordination, uploads, and release regression now require organization-scoped enrollment credentials and URLs.
* Enforced server-derived organization scope throughout R2C coordination: websocket groups, zone presence, owner leases, confirmation replay, standalone proximity grouping, recent sightings, persistence queries, cleanup, and public legacy snapshots no longer combine tenants that use the same map or Remote ID.
* Added an additive database migration that assigns existing coordination rows to the legacy namespace, stores authenticated organization IDs on new rows, and creates composite organization/map lookup indexes.
* Added regression coverage proving same-map and same-Remote-ID isolation across organizations and verifying migration of existing coordination tables.
* Added month-to-date shadow platform-cost reporting for organization owners and billing administrators, with category totals, billing-data freshness, and clear non-charge labeling.
* Ensured tagged candidate and public version pages render the complete pending release notes throughout the guarded publication workflow.

v1.4.25: deployed
* Added a low-cost guarded Cloud Run release path that blocks deployment during active operational use, tests a zero-traffic candidate against cloud databases and public protocols, and separates explicit promotion from rollback.
* Added liveness/readiness probes, a dedicated protected deployment gate, managed-video and coordination activity checks, and a release check that rejects rollback-incompatible destructive schema migrations.

v1.4.24: deployed
* Limited the public organization directory to tenants explicitly marked public; restricted organizations remain available only through their direct sign-in URLs and authenticated access.

v1.4.23: deployed
* Replaced named-team examples and interim ownership language with neutral mySAR or program-operator wording while preserving the separately directed Board review package.
* Clarified throughout the public tracker interface that RID2Caltopo is an independent project, is not affiliated with or endorsed by CalTopo, uses the CalTopo Teams API, and appreciates the CalTopo team's product and API support.

v1.4.22: deployed
* Strengthened managed-access terms to describe RID2Caltopo and R2C Tracker as best-effort, as-is services without express or implied guarantees and to require independent verification of safety-critical information.
* Fixed restored-organization invitations and Google administrator activation redirects in guarded pilot deployments.
* Disabled the retired global administration interface by default and made missing tracker API credentials fail closed.
* Restricted browser cross-origin access, enabled production HSTS, bounded archive restore uploads and expansion, added outbound request timeouts, and validated billing-export table identifiers.
* Locked the reviewed server dependency set and added authorization-inventory tests, dependency and static analysis, secret scanning, an SBOM, Dependabot, security policy, and incident, monitoring, continuity, cloud-hardening, and public-safety runbooks.
* Added the Apache 2.0 license and updated the Board technical security review and prioritized remediation package.

v1.4.21: deployed
* Required managed-access applicants to acknowledge versioned safety terms explaining that RID2Caltopo and R2C Tracker are supplemental tools, may be unavailable or incomplete, and are not the sole source for safety-critical or incident-command decisions.
* Added automated seven-day, one-day, and expiration notices for trial and grace deadlines with organization-scoped CSV and flight-log archive instructions; deadline passage does not automatically archive or shut down a site.
* Required the platform administrator to confirm and audit direct contact with an organization administrator before manually archiving and disabling an organization site.
* Paused public contribution solicitations and removed personal donation links while a fiscal sponsorship and long-term operating structure are evaluated.

v1.4.20: deployed
* Promoted the hosted evaluation to guarded live organization onboarding with permanent tenant paths, managed-access request intake, activation and access email, and tenant-scoped enrollment credentials.
* Added platform-administrator controls to edit organization and administrator contact details, replace an administrator with an accountable reactivation flow, add prepaid credit, and archive or restore an organization while keeping its designator reserved.
* Added send-only Gmail authorization for invitations and notifications plus non-enumerating, rate-limited, single-use organization password reset links; OAuth-only members continue to sign in without an R2C password.
* Added transparent funding lifecycle behavior: trials begin on administrator activation unless credit is already available, cumulative attributed GCP usage consumes deposited credit, and exhausted funded accounts enter a 30-day grace period with notifications.
* Added category-aware shadow allocation of compute, network and TURN, storage, database, and shared platform costs across eligible organizations without charging their ledgers.
* Added optional Stripe Checkout and signed-webhook support for prepaid funding, including net credit after the actual processing fee; payments remain visibly unavailable unless both Stripe secrets are configured.
* Migrated the pilot deployment path from Cloud SQL attachment to isolated PostgreSQL databases reached through guarded Direct VPC egress, and expanded Secret Manager wiring for managed requests, Gmail, and optional Stripe credentials.
* Refreshed organization and platform navigation, status and funding displays, access-request visibility, project-support messaging, and application icon treatment.
* Added secure, organization-scoped downloads for individual archived flight logs from the flight administration table.
* Fixed flight administration table overflow guidance so the fade and scroll instructions appear only when additional columns are actually off-screen.
* Reduced pilot-approved live-video startup delays with fast approval reconciliation, first-relay-candidate signaling, and privacy-safe TURN timing diagnostics.
